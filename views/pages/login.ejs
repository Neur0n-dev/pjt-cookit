<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>로그인 - Cookit</title>
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/cookit_face_32.png">
    <link rel="stylesheet" href="/css/common.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/css/footer.css?v=<%= Date.now() %>">
    <link rel="stylesheet" href="/css/login.css?v=<%= Date.now() %>">
</head>

<body>
    <main class="container login-container">
        <section class="login-card" aria-label="로그인">
            <!-- Brand -->
            <a href="/" class="login-brand" aria-label="Cookit 홈으로">
                <span class="login-icon" aria-hidden="true">🍳</span>
                <div class="login-title-wrap">
                    <h1 class="login-title">Cook<span class="login-title-accent">it</span></h1>
                    <p class="login-sub">재료만 입력하면 Cookit이 딱 맞는 메뉴를 찾아드립니다.</p>
                </div>
            </a>

            <!-- Tabs -->
            <div class="login-tabs" role="tablist" aria-label="로그인 방식">
                <button class="tab active" type="button" role="tab" aria-selected="true" data-tab="idpw">ID/비밀번호</button>
                <button class="tab" type="button" role="tab" aria-selected="false" data-tab="otp">일회용 번호</button>
                <button class="tab" type="button" role="tab" aria-selected="false" data-tab="qr">QR코드</button>
            </div>

            <!-- Panels -->
            <div class="tab-panels">
                <!-- ID/PW -->
                <div class="login-panel active" id="panel-idpw" role="tabpanel">
                    <form class="login-form" method="POST" action="/login">
                        <div class="field">
                        <label class="label" for="userId">아이디</label>
                            <div class="input-wrap">
                                <input id="userId" name="userId" type="text"placeholder="아이디를 입력하세요"autocomplete="username" required>
                                <button type="button" class="icon-btn" data-clear="#userId" aria-label="지우기">✕</button>
                            </div>
                        </div>

                        <div class="field">
                        <label class="label" for="password">비밀번호</label>
                            <div class="input-wrap">
                                <input id="password" name="password" type="password"placeholder="비밀번호를 입력하세요"autocomplete="current-password" required>
                                <button type="button" class="icon-btn" data-toggle="#password" aria-label="비밀번호 보기">👁</button>
                                <button type="button" class="icon-btn" data-clear="#password" aria-label="지우기">✕</button>
                            </div>
                        </div>

                        <!-- ERROR -->
                        <% if (typeof error !== 'undefined' && error) { %>
                            <div class="login-alert" role="alert"><%= error %></div>
                        <% } %>

                        <button class="btn-primary" type="submit">로그인</button>
                    </form>

                    <div class="login-links" aria-label="도움 링크">
                        <a href="/find-id" class="muted">아이디 찾기</a>
                        <a href="/find-pw" class="muted">비밀번호 찾기</a>
                        <a href="/join" class="muted">회원가입</a>
                    </div>
                </div>

                <!-- OTP (UI only) -->
                <div class="login-panel" id="panel-otp" role="tabpanel">
                    <div class="placeholder">
                        <h3>일회용 번호</h3>
                        <p>포트폴리오 UI 샘플입니다. (추후 구현)</p>
                        <button class="btn-ghost" type="button" disabled>일회용 번호로 로그인</button>
                    </div>
                </div>

                <!-- QR (UI only) -->
                <div class="login-panel" id="panel-qr" role="tabpanel">
                    <div class="placeholder">
                        <h3>QR코드</h3>
                        <p>포트폴리오 UI 샘플입니다. (추후 구현)</p>
                        <div class="qr-box" aria-hidden="true"></div>
                        <button class="btn-ghost" type="button" disabled>QR코드로 로그인</button>
                    </div>
                </div>
            </div>

        </section>
    </main>

    <!-- 푸터 -->
    <%- include('../partials/footer') %>

    <script src="/js/login.js?v=<%= Date.now() %>"></script>
    <script src="/js/footer.js?v=<%= Date.now() %>"></script>
</body>
</html>
